spring.application.name=SoftwareUToolsTestProjekt

# Render gibt der App ueber die Umgebungsvariable PORT einen Port vor.
# Ohne diese Zeile findet Render keinen offenen Port und die App ist nicht erreichbar.
# Lokal (wenn PORT nicht gesetzt ist) nehmen wir einfach 8080.
server.port=${PORT:8080}
